#pragma once

// Copy this file to "secrets.h" (in the same "include" folder) and fill in
// your own Wi-Fi credentials. "secrets.h" is ignored by git so your
// credentials never get committed/pushed to GitHub.

#define WIFI_SSID     "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"
